import htmlIcon from "../functions/htmlIcon";

import svg from "../uri-encoded-icons/museum-icon.svg.uri";

export default function museumIcon(message, onClick) {
    return htmlIcon(svg, message, onClick);
}
